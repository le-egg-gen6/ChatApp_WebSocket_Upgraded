spring:
  application:
    name: ChatAppNewVersion
  data:
    mongodb:
      username: your_username
      password: your_password
      host: localhost
      port: 27017
      database: chat_app
      authentication-database: admin

server:
  port: 8088
